# ARIA-RAG (LangChain document-retrieval ARIA) — OPTIONAL extra.
#
# This is a SEPARATE, optional dependency set for simulation/llm_compare/aria_langchain.py
# (the LangChain RAG demo). It is intentionally NOT merged into requirements.lock.
#
# ⚠ Install with pip, NEVER `uv sync` (uv sync rewrites requirements.lock and
#   removes undeclared deps like mlx-lm / downgrades transformers, breaking ARIA —
#   see MEMORY: uv-sync-unsafe-incident-20260613).
#
# Install:
#   .venv/bin/python -m ensurepip --upgrade        # bootstrap pip in the uv-managed venv (one-off)
#   .venv/bin/python -m pip install -r requirements-langchain.txt
#
# Embeddings reuse the already-present `sentence-transformers` (in requirements.lock);
# the LLM is a local Ollama model (qwen2.5:3b or mistral:7b) — also not a Python dep.
#
# Verified installed 2026-06-27 on Python 3.12.13 (macOS arm64); requirements.lock
# md5 unchanged after install; transformers 5.12.0 + mlx_lm intact.

langchain==1.3.11
langchain-community==0.4.2
langchain-core==1.4.8
langchain-ollama==1.1.0
langchain-chroma==1.1.0
langchain-text-splitters==1.1.2
chromadb==1.5.9
ollama==0.6.2
dspy-ai>=3.2.1  # ARIA 프롬프트 최적화 PoC (G-394, pip only)
